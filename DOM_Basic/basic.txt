DOM = Document Object Model

When we develop a dynamic website first we need is Understanding DOM.
It makes a model of HTML as an object. Then  follow  few methods to select a div or portion of HTML and manipulate it using JS.
